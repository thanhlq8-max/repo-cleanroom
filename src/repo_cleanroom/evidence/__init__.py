"""Opt-in command evidence mapping (v0.5.x).

Evidence enters only through an explicit user-supplied file, is never executed,
and is sanitized before anything is persisted. See docs/COMMAND_EVIDENCE_PRIVACY.md.
"""
