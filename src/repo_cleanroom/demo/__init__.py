"""Synthetic demo workspace generation (v0.7.x).

Everything generated here is synthetic placeholder data. The generator refuses
to write into an existing non-empty directory so it can never touch real data.
"""
